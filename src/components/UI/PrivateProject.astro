---
import type { PrivateProject } from '../../types'


interface Props {
    data: PrivateProject
}

const { data } = Astro.props
console.log(data)
---

<article class="mt-5">
    <h1>{data.title}</h1>
</article>