---

import { AstroFont } from 'astro-font'

---

<AstroFont
    config={[
        {
            name: "Monoton",
            src: [
                {
                    style: 'normal',
                    path: 'public/fonts/Monoton-Regular.ttf'
                }
            ],
            preload: true,
            display: "swap",
            selector: ".monoton",
            fallback: "sans-serif"
        }
    ]}
>

</AstroFont>